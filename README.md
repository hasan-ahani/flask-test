# Flask project
this is sample flask project for university class


# Install 
first on you should make env in python

```bash
python -m venv .venv
```

# Install project
when created venv in this project you should install packages

```bash
pip install -r requirements.txt
```

# Start Project
with this commend you can start flask

```bash
python main.py
```